import SwiftUI

/// Settings tab root (owner: Settings/Onboarding UI agent).
struct SettingsScreen: View {
    var body: some View {
        List {}
            .navigationTitle(Copy.Settings.Root.title)
    }
}
