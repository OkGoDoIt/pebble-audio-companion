import SwiftUI

/// Library tab root (owner: Library/Conversation UI agent).
struct LibraryScreen: View {
    var body: some View {
        List {}
            .navigationTitle(Copy.Library.title)
    }
}
